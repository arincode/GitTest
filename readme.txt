GIT CONFIGURATION:
git config --global user.name "arincode"
git config --global user.email "arin.ch.dev@gmail.com"
git config --list 
git config user.name 
git config user.email
--------------------------------------------------
To check status:
git status
git log
--------------------------------------------------
Create repository:
git init 

To Stage:
git add --all 

To commit:
git commit -m "Initial commit" 